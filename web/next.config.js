/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "standalone",
    reactStrictMode: false,
    compiler: {
        styledComponents: true
    }
}

module.exports = nextConfig


